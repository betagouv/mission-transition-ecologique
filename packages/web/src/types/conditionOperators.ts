export enum ConditionOperators {
  or = 'or',
  and = 'and',
  is = '==',
  exists = 'exists',
  inexists = 'inexists',
  notEqual = '!=',
  superior = '>',
  superiorOrEqual = '>=',
  inferior = '<',
  inferiorOrEqual = '<='
}
