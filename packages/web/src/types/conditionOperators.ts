export enum ConditionOperators {
  or = 'or',
  and = 'and',
  is = '==',
  exists = 'exists',
  missing = 'missing',
  notEqual = '!=',
  superior = '>',
  superiorOrEqual = '>=',
  inferior = '<',
  inferiorOrEqual = '<='
}
